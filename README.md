This program picks a random number and compares it against an input number from the end user. The program outputs if the user's input is higher,
lower, or the same number as the random number generated.
